{
  "name": "my-spa",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf docs && vite build && mv dist docs"
  },
  "dependencies": {
    "universal-router": "^10.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}